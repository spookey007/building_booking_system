import { PaymentScheduleWorkspace } from "./payment-schedule-workspace";

export const metadata = {
  title: "Payment schedule · FM Towers",
};

export default function PaymentSchedulePage() {
  return <PaymentScheduleWorkspace />;
}
