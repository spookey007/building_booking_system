import { PaymentScheduleReportsWorkspace } from "./payment-schedule-reports-workspace";

export const metadata = {
  title: "Payment schedule (demo) · Reports · FM Towers",
};

export default function PaymentScheduleReportPage() {
  return <PaymentScheduleReportsWorkspace />;
}
